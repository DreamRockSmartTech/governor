#!/usr/bin/env sh
# Governor commit-msg — review-boundary check (control 6): one staged WorkItem
# node per commit; stamps the Governor-WorkItem binding trailer.
# This repo runs Governor from source (no installed binary).
deno run -A cli/mod.ts review-check "$1"
